{% extends 'base.html.twig' %}

{% block title %}{{ 'app.customer.edit_customer'|trans }}{% endblock %}

{% block body %}
<div class="container">
    <div class="flex items-center justify-center min-h-screen">
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-1/2">
            <h1>{{ 'app.customer.new_customer'|trans }}</h1>

            {{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="button btn-center" href="{{ path('admin_customer_index') }}">{{ 'app.default.back_to_list'|trans }}</a>

            {{ include('customer/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
