{% extends 'base.html.twig' %}

{% block title %}{{ 'app.invoice.edit_invoice'|trans }}{% endblock %}

{% block body %}
<div class="container">
    <div class="flex items-center justify-center min-h-screen">
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-1/2">
            <h1>{{ 'app.invoice.edit_invoice'|trans }}</h1>

            {{ include('invoice/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="button" href="{{ path('admin_invoice_index') }}">{{ 'app.default.back_to_list'|trans }}</a>

            {{ include('invoice/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
