{% for type, messages in app.session.flashBag.all %}
    {% for message in messages %}
        <div class="message-text {{ type == 'error' ? 'error-message' : '' }} mb-3" role="alert">
            {{ message|raw|nl2br }}
        </div>
    {% endfor %}
{% endfor %}
