{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
<div class="container">
    <div class="flex items-center justify-center pt-5">
        <div class="bg-gray-800 p-8">
            <h1>Admin</h1>
            <div>
                <a class="button" href="{{ path('admin_customer_index') }}">{{ 'app.customer.title'|trans }}</a>
                <a class="button" href="{{ path('admin_project_index') }}">{{ 'app.project.title'|trans }}</a>
                <a class="button" href="{{ path('admin_invoice_index') }}">{{ 'app.invoice.title'|trans }}</a>
                <a class="button" href="{{ path('admin_time_entry_index') }}">{{ 'app.time_entry.title'|trans }}</a>
                <a class="button" href="{{ path('admin_email_signature_index') }}">{{ 'app.email_signature.title'|trans }}</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
